{
  "name": "business-planner-alpha",
  "version": "0.1.0-alpha",
  "private": true,
  "description": "Business Planner BUILD_ALPHA — MVP local, 1 vertical (Comercial+COM-EXP), agente mock, persistencia local.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "start:prod": "prisma db push --skip-generate && next start",
    "postinstall": "prisma generate",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev --name init",
    "prisma:studio": "prisma studio"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.106.0",
    "@geoman-io/leaflet-geoman-free": "^2.20.0",
    "@prisma/client": "^6.19.0",
    "@types/leaflet": "^1.9.21",
    "@types/three": "^0.185.1",
    "leaflet": "^1.9.4",
    "next": "16.2.9",
    "react": "19.2.7",
    "react-dom": "19.2.7",
    "three": "^0.185.1"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "prisma": "^6.19.0",
    "typescript": "^5.6.0"
  },
  "comment": "Persistencia PostgreSQL+Prisma (R5D: intercambiable). Deploy en Railway (Nixpacks, start:prod hace db push). Prisma solo server-side."
}
